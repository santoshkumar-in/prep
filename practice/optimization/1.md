# Optimization — 20 Practice Questions
## No solutions — Answer key at the bottom

---

### Q1 [MCQ]
Given a continuous and twice differentiable function, which of the following is a **necessary** condition for a minimum at $x = c$?

(a) $f''(c) > 0$
(b) $f'(c) = 0$
(c) $f'(c) > 0$
(d) $f''(c) = 0$

---

### Q2 [MCQ]
The function $f(x) = x^3 - 3x + 2$ has a local maximum at:

(a) $x = 1$
(b) $x = -1$
(c) $x = 0$
(d) $x = 2$

---

### Q3 [MSQ]
Which of the following functions are convex?

(a) $f(x) = e^x$
(b) $f(x) = -x^2$
(c) $f(x) = |x|$
(d) $f(x) = \log x$ for $x > 0$
(e) $f(x) = x^4$

---

### Q4 [MCQ]
The maximization of $f(x)$ is equivalent to the minimization of:

(a) $|f(x)|$
(b) $f(x)^2$
(c) $-f(x)$
(d) $1/f(x)$

---

### Q5 [NUM]
Apply one step of Newton's method to minimize $f(x) = x^2 - 6x + 8$ starting from $x_0 = 0$.

---

### Q6 [MSQ]
Which of the following are TRUE about gradient descent?

(a) The learning rate $\alpha$ is a scalar
(b) A very large learning rate guarantees faster convergence
(c) For a strictly convex function, gradient descent converges to the global minimum
(d) The gradient points in the direction of steepest descent

---

### Q7 [MCQ]
$f(x, y) = x^2 + 4y^2 - 4x - 8y + 8$. The critical point is:

(a) $(1, 2)$
(b) $(2, 1)$
(c) $(4, 2)$
(d) $(2, 4)$

---

### Q8 [MCQ]
The Hessian of $f(x_1, x_2) = 3x_1^2 + 2x_1x_2 + x_2^2$ is:

(a) $\begin{bmatrix}6&2\\2&2\end{bmatrix}$

(b) $\begin{bmatrix}3&1\\1&1\end{bmatrix}$

(c) $\begin{bmatrix}6&1\\1&2\end{bmatrix}$

(d) $\begin{bmatrix}3&2\\2&1\end{bmatrix}$

---

### Q9 [MSQ]
For the critical point of a multivariate function, which of the following correctly classify it?

(a) Hessian positive definite → local minimum
(b) Hessian negative definite → local maximum
(c) Hessian indefinite → saddle point
(d) Hessian positive definite → local maximum

---

### Q10 [NUM]
Perform 3 steps of gradient descent on $f(x) = 2x^2 - 8x$ starting from $x_0 = 0$ with learning rate $\eta = 0.1$.

---

### Q11 [MCQ]
Consider $f(x) = x^4 - 4x^2$. Which of the following is TRUE?

(a) $f$ has one local minimum at $x = 0$
(b) $f$ has two local minima at $x = \pm\sqrt{2}$ and one local maximum at $x = 0$
(c) $f$ has no local extrema
(d) $f$ has two local maxima at $x = \pm\sqrt{2}$

---

### Q12 [MCQ]
The OLS objective $f(\boldsymbol{\beta}) = \|A\boldsymbol{\beta} - \mathbf{y}\|^2$ is:

(a) Non-convex — multiple local minima exist
(b) Always convex regardless of $A$
(c) Convex only when $A$ is square
(d) Convex only when $A$ has full column rank

---

### Q13 [MSQ]
Setting $\nabla f(\boldsymbol{\beta}) = \mathbf{0}$ for $f(\boldsymbol{\beta}) = \|A\boldsymbol{\beta} - \mathbf{y}\|^2$ gives which of the following?

(a) $A^TA\hat{\boldsymbol{\beta}} = A^T\mathbf{y}$
(b) $A\hat{\boldsymbol{\beta}} = \mathbf{y}$
(c) $\hat{\boldsymbol{\beta}} = (A^TA)^{-1}A^T\mathbf{y}$ when $A$ has full column rank
(d) $\hat{\boldsymbol{\beta}} = A^{-1}\mathbf{y}$

---

### Q14 [MCQ]
Consider the two optimization problems:

**Problem A:** $\min f(x)$ subject to $\alpha_i \leq x_i \leq \beta_i$

**Problem B:** $\min f(x)$ subject to $\alpha_i \leq x_i \leq \beta_i$ AND $g_k(x) = 0$

Which is correct?

(a) Both are constrained
(b) Both are unconstrained
(c) A is unconstrained, B is constrained
(d) A is constrained, B is unconstrained

---

### Q15 [NUM]
For $f(x, y) = x^2 + xy + y^2 - 3x$, find the critical point and classify it using the Hessian.

---

### Q16 [MSQ]
Which of the following are TRUE about Newton's method for minimisation?

(a) Update rule is $x_{k+1} = x_k - \frac{f'(x_k)}{f''(x_k)}$
(b) It converges quadratically near the solution
(c) It requires computing the Hessian inverse in multivariate case
(d) It always converges regardless of starting point

---

### Q17 [MCQ]
Suppose $f(x)$ is a convex function defined on $[a, b]$ and $f'(c) = 0$ at an interior point $c$. Then:

(a) $c$ is a saddle point
(b) $c$ is a local but not global minimum
(c) $f$ has multiple global extrema in $[a, b]$
(d) $c$ is a local maximum

---

### Q18 [MSQ]
The Ridge regression objective is $f(\boldsymbol{\beta}) = \|A\boldsymbol{\beta} - \mathbf{y}\|^2 + \lambda\|\boldsymbol{\beta}\|^2$. Which are TRUE?

(a) Setting $\nabla f = \mathbf{0}$ gives $(A^TA + \lambda I)\hat{\boldsymbol{\beta}} = A^T\mathbf{y}$
(b) The objective is strictly convex for any $\lambda > 0$
(c) As $\lambda \to \infty$, $\hat{\boldsymbol{\beta}} \to \mathbf{0}$
(d) At $\lambda = 0$ it reduces to OLS

---

### Q19 [MCQ]
Stochastic Gradient Descent (SGD) differs from batch gradient descent in that:

(a) SGD uses the exact gradient at each step
(b) SGD uses one randomly selected sample to estimate the gradient
(c) SGD always converges faster to the exact minimum
(d) SGD cannot be used for non-convex problems

---

### Q20 [MSQ]
For $f(x) = x^{2/3}$ on $[-1, 1]$. Select all TRUE statements:

(a) $f'(0)$ is undefined
(b) $x = 0$ is a local and global minimum
(c) $f'(0) = 0$
(d) Global maxima occur at $x = \pm 1$
(e) The second derivative test confirms $x=0$ is a minimum

---

---

## ANSWER KEY

| Q | Answer |
|---|--------|
| 1 | **(b)** $f'(c)=0$ — necessary condition |
| 2 | **(b)** $x=-1$ — $f''(-1)=-6<0$ |
| 3 | **(a), (c), (e)** |
| 4 | **(c)** $-f(x)$ |
| 5 | $x_1 = x_0 - \frac{f'(x_0)}{f''(x_0)} = 0 - \frac{-6}{2} = \mathbf{3} = x^*$ (converges in 1 step) |
| 6 | **(a) and (c)** |
| 7 | **(b)** $(2,1)$ |
| 8 | **(a)** $\begin{bmatrix}6&2\\2&2\end{bmatrix}$ |
| 9 | **(a), (b), (c)** |
| 10 | $x_1=0.8,\ x_2=1.44,\ x_3=1.888$ |
| 11 | **(b)** |
| 12 | **(b)** Always convex |
| 13 | **(a) and (c)** |
| 14 | **(c)** A unconstrained, B constrained |
| 15 | Critical point $(2, -1)$; Hessian PD → **local minimum** |
| 16 | **(a), (b), (c)** |
| 17 | **(c)** Multiple global extrema |
| 18 | **(a), (b), (c), (d)** |
| 19 | **(b)** |
| 20 | **(a), (b), (d)** |